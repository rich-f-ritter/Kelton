openpyxl>=3.1
zipcodes>=1.2
# The companion HTML map (scripts/build_map.py) bundles Leaflet from
# assets/vendor/ and needs no extra Python packages — only network access at
# view time for the satellite basemap tiles.
